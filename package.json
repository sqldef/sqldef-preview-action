{
  "name": "sqldef-preview-action",
  "version": "1.0.0",
  "description": "Preview database schema migrations using sqldef tools",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "package": "npm run build && npm run package",
    "test": "jest",
    "lint": "eslint src/**/*.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gfx/sqldef-preview-action.git"
  },
  "keywords": [
    "github-action",
    "sqldef", 
    "database",
    "migration",
    "schema"
  ],
  "author": "FUJI Goro",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@actions/exec": "^1.1.1",
    "@actions/tool-cache": "^2.0.1",
    "@actions/http-client": "^2.2.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  }
}